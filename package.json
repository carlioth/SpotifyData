{
  "name": "getspotifydata",
  "version": "0.0.1",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "npm run compile && node dist/src/index.js",
    "parse": "npm run compile && node dist/src/parse.js",
    "compile": "rimraf dist/* && babel src --out-dir dist/src --copy-files --source-maps",
    "clean": "rimraf dist/*"
  },
  "author": "Henrik Carlioth",
  "license": "MIT",
  "dependencies": {
    "babel-cli": "^6.24.0",
    "spotify-web-api-node": "^2.3.6"
  },
  "devDependencies": {
    "babel-preset-env": "^1.2.2",
    "babel-preset-stage-0": "^6.22.0",
    "rimraf": "2.6.1"
  }
}